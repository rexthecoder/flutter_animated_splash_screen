name: animated_native_splash
description: A package which automate the task creating of animated splash screen in flutter.
version: 1.0.0
homepage: https://github.com/Rexfordasamoah51/flutter_animated_splash_screen
maintainer: Rexford Asamoah 

environment:
   sdk: ">=2.7.0 <3.0.0"
   flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  yaml: ^3.0.0
  xml: ^5.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic: ^1.10.0

flutter: 
   uses-material-design: true
